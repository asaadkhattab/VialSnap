@model Vialsnap.Models.PatientListViewModel



@{
    ViewBag.Title = "People";
}

<h2>People</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @for (var i = 0; i < Model.People.Count; i++)
        {
            <tr>
                <td>@Model.People[i].FullName</td>
                <td>
                    @Html.ActionLink("Detail", "PatientDetail", new { id = Model.People[i].PatientId })
                  - @Html.ActionLink("Edit", "PatientEdit", new { id = Model.People[i].PatientId })
                  - <a data-toggle="modal" href="@("#deletePatientModal" + i)">Delete</a>
                    <div class="modal fade" id="@("deletePatientModal" + i)" tabindex="-1" role="dialog">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                @using (Html.BeginForm("DeletePatient", "Patient"))
                                {
                                    <input type="hidden" value="@Model.People[i].PatientId" name="PatientId" />
                                    <div class="modal-header">
                                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                                        <h4 class="modal-title">Delete @Model.People[i].FullName</h4>
                                    </div>
                                    <div class="modal-body">
                                        Are you sure you want to delete @Model.People[i].FullName?
                                    </div>
                                    <div class="modal-footer">
                                        <button type="submit" class="btn btn-primary">Yes</button>
                                        <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>


<div class="col-sm-12 form-group">
    <label>Total People:</label> @Model.TotalPeople
</div>

<div class="col-sm-12">
    @Html.ActionLink("Add Patient", "PatientAdd", null, new { @class = "btn btn-primary" })
</div>
